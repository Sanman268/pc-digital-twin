# 3D Models

Place your exported PC case here as `pc_case.glb`.

Mesh names must match the convention in `../../README.md` so the renderer can
locate them via `getObjectByName()`:

- `case_body`
- `side_panel`
- `fan_front`
- `fan_rear`
- `gpu`
- `cpu_area`
- `psu`
- `sensor_node`
